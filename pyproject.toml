[project]
name = "eurostat-tam-mcp"
version = "0.1.0"
description = "MCP server pulling Eurostat SBS enterprise counts (SME / 250+) by NACE Rev.2 for DTM's TAM model"
requires-python = ">=3.10"
dependencies = ["mcp[cli]>=1.2.0", "openpyxl>=3.1"]

[tool.uv]
package = false
